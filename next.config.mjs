/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "via.placeholder.com"], // Allowed external image domains
  },
};

export default nextConfig;
